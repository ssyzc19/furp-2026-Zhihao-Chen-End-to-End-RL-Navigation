# Meeting Notes — Week 1

**Date:** 2026-06-11
**Type:** Weekly meeting
**Attendees:** _（待填：学生姓名 / 导师 / project lead）_

## Discussion summary
- 确认项目主方向：**Track 2 — End-to-End RL Navigation for AMR（Habitat PointNav）**，先在无语言的导航任务上跑通 RL 训练流程，后续再考虑 VLN。
- 回顾 Week 1 环境与 smoke test 进度：CartPole PPO（SB3，reward 稳定 500）跑通；Habitat-Sim / Habitat-Lab 0.3.3 安装并 import 成功；PointNav 测试数据集就位。
- 复盘本周最大 blocker：WSL2 + RTX 4060 下 `CameraSensor` 无法创建 EGL 渲染上下文（`unable to find CUDA device 0 among 1 EGL devices`，`glxinfo` 落到 `llvmpipe` 软件渲染）。用分层测试法（Dataset→Sim→Scene→Sensor）定位到故障在渲染链路，而非 RL / Habitat-Lab 本身。
- 确认 `shortest_path_follower_example.py` 已在云 GPU 上成功运行并生成带 RGB/Depth 渲染的 `trajectory.mp4`，Week 1 smoke test 全链路打通。

## Decisions made
- **环境策略**：不在 WSL2 EGL 上死磕，也不重建 conda / 不套 Docker；**渲染与训练统一走云 GPU 实例（RTX A4000 / 原生 Ubuntu 22.04）**，本地 WSL2 仅做读码 / CartPole / 数据集中转。
- **数据集获取**：云端 HuggingFace 超时 → 采用「本地 WSL → Windows 桌面 → Jupyter 上传 → 云服务器」中转方案。
- **Week 1 判定**：环境 + smoke test 证据齐全，**Week 1 100% 达标**，正式进入 Week 2。
- **Week 2 主线**：从「只读文献」调整为 **跑通官方 PointNav PPO Baseline + 读懂 Baselines 架构 + 建立 CartPole↔PointNav 对应**，文献作为辅助背景。

## Action items
- [ ] Goal 1 — 跑通官方 PointNav PPO Baseline（小步数 `total_num_steps≈5e4–1e5`、`num_environments=4`），拿到训练日志 / TensorBoard 曲线 / ckpt / eval SR·SPL — owner: 学生 — due: Week 2
- [ ] Goal 2 — 读懂 Habitat Baselines 架构（`ppo_trainer.py` / `policy.py` / `rollout_storage.py` / reward / observation） — owner: 学生 — due: Week 2
- [ ] Goal 3 — 产出 CartPole PPO ↔ PointNav PPO 对应表 + `Obs→PPO→Action→Env→Reward` 数据流图 — owner: 学生 — due: Week 2
- [ ] 补齐 habitat-lab commit hash（`git -C ~/habitat-lab rev-parse HEAD`）归档以满足可复现要求 — owner: 学生 — due: Week 2
- [ ] 归档 `nvidia-smi` 输出截图与 EGL 排查证据到仓库 — owner: 学生 — due: Week 2

## Questions / follow-ups
- 云 GPU 实例的使用时长 / 预算是否有限制？Week 3 若要拉长 `total_num_steps` 做一次像样的收敛，算力是否够用？
- PointNav baseline 的目标指标范围（SR / SPL 合理区间）是多少，便于判断训练是否正常收敛？
- 「Research Track 至少 10% 创新」的改进点，是否需要在 Week 2 末先定下候选方向（如奖励塑形 / 观测设计 / 记忆模块）？
